@startuml ACO+2-opt局部搜索流程图
title ACO + 2-opt 局部搜索流程

skinparam backgroundColor #FEFEFE
skinparam activityBackgroundColor #E8F4FD
skinparam activityBorderColor #4A90D9
skinparam activityDiamondBackgroundColor #FFE4B5

start

:初始化参数;
note right
  • 距离矩阵
  • 信息素矩阵
  • 2-opt优化器
end note

:iteration = 1;

repeat

  :蚁群构建n条路径;

  #LightYellow:2-opt局部搜索（精英蚂蚁）;
  note right
    对前10%的精英路径：

    while(有改善):
      for i, j in 所有边对:
        old = dist(A,B) + dist(C,D)
        new = dist(A,C) + dist(B,D)

        if new < old:
          反转 B...C 段
          有改善 = True
          break

    ─────────────────
    图示：
    A ─→ B ─→ ... ─→ C ─→ D
         ╲           ╱
          ╲─────────╱  (交叉)

    A ─→ C ─→ ... ─→ B ─→ D
          (无交叉)
  end note

  if (当前最优 < 全局最优?) then (是)
    :更新全局最优解;
  else (否)
  endif

  :信息素更新;

  :记录收敛数据;

  :iteration++;

repeat while (iteration < max_iter?) is (是)
->否;

:输出结果;

stop

@enduml
